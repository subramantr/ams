<%= form_for(@approval_state) do |f| %>
  <% if @approval_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@approval_state.errors.count, "error") %> prohibited this approval_state from being saved:</h2>

      <ul>
      <% @approval_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :approval_state_name %><br />
    <%= f.text_field :approval_state_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
